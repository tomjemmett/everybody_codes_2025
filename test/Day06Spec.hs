module Day06Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 6" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day06 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day06 "actual"
  --   a `shouldBe` ()