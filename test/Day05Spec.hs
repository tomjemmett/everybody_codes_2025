module Day05Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 5" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day05 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day05 "actual"
  --   a `shouldBe` ()