module Day08Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 8" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day08 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day08 "actual"
  --   a `shouldBe` ()