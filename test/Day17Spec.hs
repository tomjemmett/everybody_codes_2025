module Day17Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 17" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day17 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day17 "actual"
  --   a `shouldBe` ()