module Day18Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 18" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day18 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day18 "actual"
  --   a `shouldBe` ()