module Day09Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 9" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day09 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day09 "actual"
  --   a `shouldBe` ()