module Day03Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 3" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day03 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day03 "actual"
  --   a `shouldBe` ()