module Day14Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 14" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day14 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day14 "actual"
  --   a `shouldBe` ()