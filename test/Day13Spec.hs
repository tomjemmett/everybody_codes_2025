module Day13Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 13" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day13 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day13 "actual"
  --   a `shouldBe` ()