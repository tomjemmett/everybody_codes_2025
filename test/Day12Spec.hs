module Day12Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 12" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day12 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day12 "actual"
  --   a `shouldBe` ()