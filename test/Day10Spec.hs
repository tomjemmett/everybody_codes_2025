module Day10Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 10" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day10 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day10 "actual"
  --   a `shouldBe` ()