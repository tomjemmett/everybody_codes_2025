module Day11Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 11" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day11 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day11 "actual"
  --   a `shouldBe` ()