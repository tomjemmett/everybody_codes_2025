module Day19Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 19" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day19 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day19 "actual"
  --   a `shouldBe` ()