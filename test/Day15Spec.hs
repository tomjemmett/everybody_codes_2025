module Day15Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 15" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day15 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day15 "actual"
  --   a `shouldBe` ()