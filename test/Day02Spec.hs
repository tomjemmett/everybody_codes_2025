module Day02Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 2" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day02 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day02 "actual"
  --   a `shouldBe` ()