module Day04Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 4" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day04 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day04 "actual"
  --   a `shouldBe` ()