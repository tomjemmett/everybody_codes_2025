module Day07Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 7" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day07 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day07 "actual"
  --   a `shouldBe` ()