module Day16Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 16" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day16 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day16 "actual"
  --   a `shouldBe` ()