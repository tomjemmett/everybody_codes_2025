module Day20Spec (spec) where

import SpecHelper

spec :: Spec
spec = describe "Day 20" $ do
  pure ()
  -- it "Sample" $ do
  --   s <- day20 "sample"
  --   s `shouldBe` ()

  -- it "Actual" $ do
  --   a <- day20 "actual"
  --   a `shouldBe` ()